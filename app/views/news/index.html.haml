.left_column
  %h1 Новости компании

  %ul.news
    - @news_list.each do |news|
      %li
        = link_to nil, '', name: "id#{ news.id }"
        .event_date= l news.created_at.to_date
        = raw news.content
        = image_tag image_url(news.picture.item.url)

