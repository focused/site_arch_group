.full
  %h1 Контакты
  .picture
    = image_tag(@doc.section(:full).picture.main.url)
    = link_to '', @doc.section(:full).picture.url, class: 'fancybox'

  .explain
    = raw @doc.section(:full).content
